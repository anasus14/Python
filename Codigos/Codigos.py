print("Buenos dias") #Sirve para escribir una indicacion o mostrar resultados
int() #sirve para que tome la entrada como numero entero
input("¿cuantos años tienes?") #sirve para obtener una respuesta
float()#sirve para numeros decimales
int(input()) #se convierte el valor introducido a un numero entero
try:() #bloque con sentencias que se quieren ejecutar pero podria tener errores
except:()#se ejecuta cuando try falle con el error
else:() #es una parte falsa
finally:() #siempre se ejecuta cuando se quiere liberar recursos.
str() #convierte los numeros en cadena
"if " #Condicional si a
"and" #y 
lista1=[] #las listas van entre corchetes y los elementos separados por comas
f"{lista1:.1f}" #Que muestre 1 decimal y sin f 0.1
".append" #agrega el elemento en la ultima posicion
".insert 2,nombre" #inserta el elemento en la posicion 2
lista1.sort()#ordena la lista de forma ascendente
print(sorted(lista1))#ordena la lista de forma descendente.
len() #cuenta los elementos de la lista
".count" #Cuenta cuantas veces se repite el elemento
lista1.pop()#elimina el elemento de la posicion

